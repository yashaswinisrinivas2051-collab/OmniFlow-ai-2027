# Default/fallback — production URLs
# For local dev, values in .env.development override these
# For production builds, values in .env.production override these
VITE_API_URL=https://omniflow-ai.onrender.com
VITE_SOCKET_URL=wss://omniflow-ai.onrender.com